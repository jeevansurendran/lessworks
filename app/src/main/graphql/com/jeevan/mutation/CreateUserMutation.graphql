mutation CreateUserMutation($name: String!, $workspaceName: String!) {
  insert_user_one(
    object: { name: $name, workspaces: { data: [{ name: $workspaceName }] } }
    on_conflict: { constraint: users_pkey, update_columns: [name] }
  ) {
    name
  }
}
